# CONTRACT — moved

**The contract lives at [`/CONTRACT.md`](../CONTRACT.md) in the repo root.**

This copy was stale and has been replaced with this pointer. It still had:

- a numbering collision, an empty `## 6. Trajectory batch` header directly
  above `## 5. Trajectory batch` holding the real content
- placeholder bounds in the field example (`32.62 / 32.24 / -110.52 / -111.00`)
  instead of the real box (`32.576089 / 32.197678 / -110.587766 / -111.069734`)
- `python prep/make_mocks.py --runs-per-batch 60`, which is not the flag that
  exists; it is `--stress`

Two contract files that disagree is the one conflict CONTRACT.md section 2
warns about, and contract lock is at 10:45. There is now one file.
