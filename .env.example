# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Daytona — https://www.daytona.io  (Person B: fleet control)
# Dashboard also shows your concurrent sandbox limit. Write that number down.
DAYTONA_API_KEY=

# OpenTopography — https://opentopography.org  (Person C: USGS 3DEP elevation)
# Free account. Key arrives by email, usually within minutes.
OPENTOPO_API_KEY=

# OpenAI — https://platform.openai.com  (Person B: model writes the movement scripts)
OPENAI_API_KEY=
